<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Doze will otherwise kill the long-poll and prompts arrive minutes
         late, or not at all. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- In-app QR scan for pairing. The camera is only ever used to read the
         `prh://` pairing code; no images leave the device. -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Android 11+ package visibility. The Core Devices app is
         `coredevices.coreapp`, NOT the classic `com.getpebble.android.basalt`
         that still appears inside its dex — naming the old one leaves the
         provider lookup returning nothing. Verified against 1.8.0.7. -->
    <queries>
        <package android:name="coredevices.coreapp" />
        <provider android:authorities="com.getpebble.android.provider.basalt" />
    </queries>

    <application
        android:theme="@style/PrhTheme"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false">
        <!--
          Cleartext is off. prh serves TLS and the companion pins its key, so
          there is no legitimate plaintext path left: pairing is scan-the-code
          only, and every request after that is HMAC-signed over pinned TLS.
          This flag is the backstop that stops an accidental plaintext
          connection from silently working.
        -->

        <activity
            android:name=".SettingsActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Pairing deep link from the extension's QR code:
                 prh://<host>:<port>?pw=<password> -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="prh" />
            </intent-filter>
        </activity>

        <!--
          The session list and per-session conversation. Not exported: they
          are reached only from within the app (the launcher is Settings, and
          Settings opens Sessions once paired). They carry no secrets and
          need no intent filters.
        -->
        <activity
            android:name=".SessionsActivity"
            android:exported="false"
            android:label="@string/sessions_title" />

        <!--
          singleTop: prompt notifications deep-link here, and a second prompt
          for the session already on screen must reuse this instance through
          onNewIntent rather than stacking another copy of the same
          conversation behind it.
        -->
        <!--
          adjustResize: the reply composer is pinned to the bottom of the
          window, so the window has to shrink for the keyboard rather than pan
          — with the default the composer is simply pushed off-screen by the
          IME it just opened.
        -->
        <activity
            android:name=".ConversationActivity"
            android:exported="false"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize"
            android:label="@string/conversation_title" />

        <service
            android:name=".PrhService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

    </application>
</manifest>
