// Versions are unverified — nothing in this checkout has a JDK or Android SDK
// to build against. Bump to whatever is current when you first build.
plugins {
    id("com.android.application") version "8.7.0" apply false
    id("org.jetbrains.kotlin.android") version "2.0.20" apply false
}
